Use `StatusMessage` for the one line that reports what just happened.

```jsx
<StatusMessage>week logged. next week adds five pounds to the squat.</StatusMessage>
<StatusMessage tone="danger">no connection. the session is saved on this device.</StatusMessage>
```

If something is neither fine nor broken, write prose instead of reaching for a third tone. Do not add amber.
