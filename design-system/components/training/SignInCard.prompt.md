Use `SignInCard` for authentication. The copy is deliberately dry.

```jsx
<SignInCard onAction={() => setSignedIn(true)} />
```

`size="lg"` is baked in — this is a primary surface. Keep the footer; it is part of the voice.
