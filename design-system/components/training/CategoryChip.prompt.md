Use `CategoryChip` to group, never to decorate. There are six axes and there will not be a seventh.

```jsx
<CategoryChip axis="legs" />
<CategoryChip axis="push">chest</CategoryChip>
```

The chip's words can be specific ("chest"); its color stays on the axis. Do not map one hue per muscle — that is the rainbow the system exists to avoid.
