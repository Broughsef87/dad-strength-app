Use `AmmoCells` inside a live set list where each set is a discrete round to spend.

```jsx
<AmmoCells total={5} spent={3} />
```

Prefer `LedBar` when you also need a label; `AmmoCells` is the compact form for a row.
